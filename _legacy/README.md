# Legacy Fastbridge site (archived)

Retired when rebuilding from Figma `1:143` hero only.

The live site is now `index.html` + `base.css` + `hero-figma.css` + `hero-motion.js`.

Contents here are not served; kept for reference and git history.
